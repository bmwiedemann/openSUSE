Index: libpcap-1.10.7/gencode.c
===================================================================
--- libpcap-1.10.7.orig/gencode.c
+++ libpcap-1.10.7/gencode.c
@@ -8684,6 +8684,7 @@ gen_inbound(compiler_state_t *cstate, in
 	 */
 	switch (cstate->linktype) {
 	case DLT_SLIP:
+	case DLT_PPP:
 		b0 = gen_relation_internal(cstate, BPF_JEQ,
 			  gen_load_internal(cstate, Q_LINK, gen_loadi_internal(cstate, 0), 1),
 			  gen_loadi_internal(cstate, 0),
