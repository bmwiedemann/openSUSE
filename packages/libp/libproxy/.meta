<package name="libproxy" project="openSUSE:Factory">
  <title>Libproxy provides consistent proxy configuration to applications</title>
  <description>libproxy offers the following features: * extremely small core
   footprint (&lt; 35K)

* no external dependencies within libproxy core (libproxy modules
   may have dependencies)
* only 3 functions in the stable external API
* dynamic adjustment to changing network topology
* a standard way of dealing with proxy settings across all scenarios</description>
  <devel project="GNOME:Factory" package="libproxy"/>
</package>
