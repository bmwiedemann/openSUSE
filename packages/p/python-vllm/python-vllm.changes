-------------------------------------------------------------------
Wed Aug 12 05:21:29 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 0.27.1:
  * Patch release on top of 0.27.0
  * Support quantized DSpark Markov heads
- Relax the exact python-compressed-tensors requirement to a
  >= 0.17.1 floor so the package survives compressed-tensors
  updates in the devel project

-------------------------------------------------------------------
Tue Aug 11 05:49:32 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 0.27.0:
  * Kimi K3 support landing as a complete stack: model files and
    kernels, Python and Rust frontends, AttnRes kernels, DeepGEMM
    support, compressed-tensors quantized checkpoints and an option
    to shard the shared expert instead of replicating it
  * Further new models: Qwen3.5 text-only dense and MoE with EVS
    video token pruning, K-EXAONE-2.0-750B-A37B, VaultGemma through
    the Transformers modeling backend, jina-embeddings-v5-text-nano
  * FlashAttention 4 on SM100 gains FP8 KV cache and headdim-256
    support, backed by new JIT and runner-owned Triton warmup
    infrastructure that removes first-request compilation stalls
  * DeepSeek-V4 performance work: sequence parallelism, roughly 2x
    kernel improvement from skipping empty c128 launches, workspace
    reuse, adaptive topk width, a compact MXFP4 indexer KV cache and
    448 MiB less GPU memory in the pipeline-parallel buffer
  * Model Runner V2 extends to non-generative workloads: encoder-only
    attention, sequence pooling for embedding and classification,
    encoder token classification and embedding, BGE-M3 pooling and
    multimodal on CPU
  * Simplified fault tolerance framework for data-parallel and
    expert-parallel deployments behind an external load balancer,
    plus async preparation for elastic expert-parallel scaling
  * Prefill/decode disaggregation for hybrid models: NIXL support for
    hybrid MLA+SSM models, heterogeneous block sizes, and MoRIIO
    heterogeneous read routing
  * The Rust frontend grows a gRPC control plane covering health
    reporting, abort control, server/model discovery and KV event
    source discovery, and integrates vllm-bench into the vllm CLI
  * CPU backend: INT8 fused MoE kernel for Arm CPUs, s390x oneDNN
    INT8 GEMM, optimised GDN conv path for speculative decoding,
    granite-4 enabled, and the CPU kernels bumped to the latest
    version
  * Quantization: FP4 Qutlass for compressed-tensors, CuTeDSL MoE for
    ReLU2 NVFP4, MXFP8 linear in INC, AutoRound W4A16 MoE, and a KV
    quant mode for TurboQuant
  * API and frontend: Cohere chat v2, cache_salt in the Anthropic
    Messages API, strict tool calling for GPT-OSS Harmony, a
    per-request stream_interval sampling parameter and a standardised
    VLLMError hierarchy
  * Early enablement for NVIDIA Rubin (sm_107) and ROCm gfx1250
  * Removed the Plamo2 and Ouro models, and the no longer supported
    max_num_partial_prefills and max_long_partial_prefills arguments
  * Upstream moved its pinned build environment to PyTorch 2.13.0;
    this package keeps building against the 2.12.0 that Factory ships
- Raise the python-mistral-common requirement to >= 1.11.6, matching
  upstream's own floor for this release
- Rebase vllm-relax-cpu-requirements.patch and
  vllm-cpu-disable-rust-frontend.patch onto 0.27.0:
  * vllm-relax-cpu-requirements.patch now relaxes the torch pin to
    >= 2.12.0, tracking what Factory ships rather than upstream's
    exact 2.13.0

-------------------------------------------------------------------
Wed Aug  5 18:26:03 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Link the CPU kernels with -Wl,-z,noexecstack: the x86 variants
  (_C, _C_AVX2, _C_AVX512) pull in assembler objects that carry no
  .note.GNU-stack, so the linker marked the whole shared object's
  stack executable and rpmlint failed the build on it

-------------------------------------------------------------------
Wed Aug  5 17:17:38 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Build vLLM's optimised C++ CPU kernels as a new "cpu" multibuild
  flavour (python-vllm-cpu, VLLM_TARGET_DEVICE=cpu), alongside the
  existing pure-Python default flavour:
  * The two flavours install the same importable vllm module, so the
    cpu one Conflicts with the default; install one or the other
  * The default flavour stays noarch and unchanged
- Bundle the sources the CPU kernels build from, since upstream's
  cmake/cpu_extension.cmake fetches them over the network at configure
  time, which an offline build root cannot do:
  * oneDNN, built statically, at upstream's two exact pins (a post-3.10
    snapshot on the aarch64/ACL path, the 3.10 tag on x86_64)
  * Arm Compute Library 52.6.0 as oneDNN's aarch64 backend, which
    upstream enables unconditionally when ASIMD is detected
  * Both are wired up through the FETCHCONTENT_SOURCE_DIR_ONEDNN and
    ACL_ROOT_DIR environment overrides upstream already honours
- Add protobuf to BuildRequires: torch's Caffe2Config.cmake calls
  find_package(Protobuf) and hard-fails without it

-------------------------------------------------------------------
Sat Jul 25 19:30:45 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 0.26.0:
  * New Inkling model family support including speculative
    decoding, LoRA and quantization; further new models:
    BertForMaskedLM, Roberta/XLMRoberta token classification,
    Cosmos3 Edge Reasoner, TranslateGemma-12b-it
  * DeepSeek-V4 performance push: specialized routing kernel,
    fused_topk_bias kernel, sparse decode/prefill optimizations
  * fp32 lm_head for generation models via head_dtype, also on
    the LoRA path
  * Attention backend selectable per KV-cache group and
    sliding-window support as an explicit backend capability
  * Matured KV offloading and tiered secondary storage:
    offloading metrics, object-store secondary tier,
    encoder-cache connectors with CPU offloading
  * Transformers 5.13 support with more models on the
    Transformers modeling backend
  * Requirements: diskcache and flashinfer-cubin dropped
- Rebase vllm-relax-cpu-requirements.patch onto the 0.26.0
  requirements (diskcache line removed upstream)
- Drop the python-diskcache runtime requirement accordingly

-------------------------------------------------------------------
Wed Jul 22 13:46:53 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to 0.25.1
- Both CPU patches rebased cleanly; dependency set unchanged from
  0.25.0

-------------------------------------------------------------------
Sat Jul 18 18:34:40 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Track the python-compressed-tensors 0.17.1 update: require
  python-compressed-tensors = 0.17.1 (API-compatible patch
  release; the quantization code path is unaffected).

-------------------------------------------------------------------
Thu Jul 16 17:24:02 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Fix runtime requirements to use canonical openSUSE package
  names so the package is installable in staging (reported by
  dimstar on sr#1365523):
  * python-pillow -> python-Pillow
  * python-pyyaml -> python-PyYAML
  * python-mistral_common -> python-mistral-common
  * python-prometheus_client -> python-prometheus-client

-------------------------------------------------------------------
Mon Jul 13 06:48:15 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Initial package of vllm 0.25.0 (CPU variant):
  * Built with VLLM_TARGET_DEVICE=empty: inference runs through
    PyTorch's native CPU operators.  The CUDA/GPU kernels and vLLM's
    optional compiled CPU kernels are not built, and the nvidia-*/
    flashinfer runtime dependencies are not pulled in.
  * Only the primary python3 flavour is built (not python-singlespec),
    since vllm is tied to the flavour python-torch is built for.
  * Add vllm-relax-cpu-requirements.patch: relax the exact torch,
    numba and setuptools pins to what Factory ships and drop the
    optional torchvision, torchaudio, torchcodec and intel-openmp
    dependencies (text inference only for this first build).
  * Add vllm-cpu-disable-rust-frontend.patch: do not build the
    optional Rust frontend (~575 unvendored crates); it is a
    runtime-optional, lazily-imported accelerator and the Python
    implementations remain fully functional.
