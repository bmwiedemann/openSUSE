<package name="python-Jinja2" project="openSUSE:Factory">
  <title>A fast and easy to use template engine written in pure Python</title>
  <description>Jinja2 is a template engine written in pure Python.  It provides a Django
inspired non-XML syntax but supports inline expressions and an optional
sandboxed environment.  Here a small example of a Jinja template:

    {% extends 'base.html' %}
    {% block title %}Memberlist{% endblock %}
    {% block content %}
      &lt;ul&gt;
      {% for user in users %}
        &lt;li&gt;&lt;a href=&quot;{{ user.url }}&quot;&gt;{{ user.username }}&lt;/a&gt;&lt;/li&gt;
      {% endfor %}
      &lt;/ul&gt;
    {% endblock %}</description>
  <devel project="devel:languages:python" package="python-Jinja2"/>
</package>
