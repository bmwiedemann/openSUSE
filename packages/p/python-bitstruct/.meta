<package name="python-bitstruct" project="openSUSE:Factory">
  <title>Interpret strings as packed binary data</title>
  <description>This module is intended to have a similar interface as the python struct
module, but working on bits instead of primitive data types (char, int, ...).

</description>
  <devel project="devel:languages:python" package="python-bitstruct"/>
  <url>https://github.com/eerimoq/bitstruct</url>
</package>
