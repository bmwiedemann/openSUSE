Index: zignal-0.6.0/zignal/music/scales.py
===================================================================
--- zignal-0.6.0.orig/zignal/music/scales.py
+++ zignal-0.6.0/zignal/music/scales.py
@@ -56,7 +56,7 @@ def piano_freq2key(f, a=49, tuning=440,
     key = 12*np.log2(f/tuning) + a
 
     if quantise:
-        key = np.int(np.round(key))
+        key = int(np.round(key))
 
     return key
 
