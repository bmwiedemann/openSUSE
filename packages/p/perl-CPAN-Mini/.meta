<package name="perl-CPAN-Mini" project="openSUSE:Factory">
  <title>create a minimal mirror of CPAN</title>
  <description>CPAN::Mini provides a simple mechanism to build and update a minimal mirror
of the CPAN on your local disk. It contains only those files needed to
install the newest version of every distribution. Those files are:

* *

  01mailrc.txt.gz

* *

  02packages.details.txt.gz

* *

  03modlist.data.gz

* *

  the last non-developer release of every dist for every author</description>
  <devel project="devel:languages:perl" package="perl-CPAN-Mini"/>
</package>
