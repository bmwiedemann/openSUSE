<package name="python-oscrypto" project="openSUSE:Factory">
  <title>python-oscrypto</title>
  <description>TLS (SSL) sockets, key generation, encryption, decryption, signing, verification and KDFs using the OS crypto libraries. Does not require a compiler, and relies on the OS for patching. Works on Windows, OS X and Linux/BSD.</description>
  <devel project="devel:languages:python" package="python-oscrypto"/>
  <url>https://github.com/wbond/oscrypto</url>
</package>
