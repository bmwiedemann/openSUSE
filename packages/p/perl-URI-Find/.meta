<package name="perl-URI-Find" project="openSUSE:Factory">
  <title>Find URIs in arbitrary text</title>
  <description>This module does one thing: Finds URIs and URLs in plain text. It finds
them quickly and it finds them all (or what URI::URL considers a URI to
be.) It only finds URIs which include a scheme (http:// or the like).

Author:	Michael G Schwern &lt;schwern@pobox.com&gt; with insight from
Uri Gutman, Greg Bacon, Jeff Pinyan, Roderick Schertler and others.</description>
  <devel project="devel:languages:perl" package="perl-URI-Find"/>
  <url>http://search.cpan.org/dist/URI-Find/</url>
</package>
