<package name="py-fishcrypt" project="openSUSE:Factory">
  <title>FiSH/Mircryption-compatible encryption for XChat/HexChat</title>
  <description>FiSH/Mircryption-compatible encryption for XChat/HexChat.

To make FiSH work with py-fishcrypt please do symlink as user

    su user

    For HexChat

    ln -sf %{_datadir}/%{name}/*.py ~/.config/hexchat/addons/


    For XChat

    ln -sf %{_datadir}/%{name}/*.py ~/.xchat2/

    exit</description>
  <devel project="network" package="py-fishcrypt"/>
</package>
