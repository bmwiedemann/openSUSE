<package name="perl-Mail-Sender" project="openSUSE:Factory">
  <title>module for sending mails with attachments through an SMTP server</title>
  <description>'Mail::Sender' provides an object oriented interface to sending mails. It
doesn't need any outer program. It connects to a mail server directly from
Perl, using Socket.

Sends mails directly from Perl through a socket connection.</description>
  <devel project="devel:languages:perl" package="perl-Mail-Sender"/>
  <url>http://search.cpan.org/dist/Mail-Sender/</url>
</package>
