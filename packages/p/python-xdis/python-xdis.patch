Remove power from the folks who brought you linux2
--- a/xdis/op_imports.py
+++ b/xdis/op_imports.py
@@ -151,6 +151,7 @@ def get_opcode_module(version_info=None,
         version_info = [int_vers // 10, int_vers % 10]
 
     vers_str = '.'.join([str(v) for v in version_info[0:3]])
+    vers_str = '.'.join([str(v) for v in version_info[0:2]])
     if len(version_info) >= 3 and version_info[3] != 'final':
         vers_str += ''.join([str(v) for v in version_info[3:]])
     if variant is None:
