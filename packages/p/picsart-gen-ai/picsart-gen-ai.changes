-------------------------------------------------------------------
Wed Sep  2 06:30:05 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 2.73.0:
  * Add the Captions, MiniMax H3 Max Ref-to-Video and Muse Image
    1.0 models, taking the catalogue from 202 to 205 entries
  * Captions is a new provider that transcribes a vertical video
    and burns in animated captions from 67 style templates
  * Add the reasoning-strength, moderation, enable-image-search,
    enable-web-search and enable-shell parameters, all of them on
    the new Muse Image 1.0 model
  * Declare a 300px minimum short side on the Seedance 2.0 and 2.5
    reference image and video slots; it is catalogue metadata only,
    nothing in the CLI enforces it
  * 2.73.0 itself only bumps the version string, its cli.js is
    identical to 2.72.1
- Re-vendor for the new release: the dependency set is still
  bit-identical to 2.70.0, so node_modules.obscpio,
  node_modules.spec.inc and node_modules.sums are unchanged and the
  licence set of the 71 vendored packages still holds

-------------------------------------------------------------------
Tue Sep  1 05:53:35 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 2.71.1:
  * Add the MiniMax H3 Max and Qwen 3.0 Pro models, taking the
    catalogue from 200 to 202 entries
  * Rename the hailuo-03 model id to minimax-h3 to match its
    modelId; scripts passing the old id need updating
  * Correct the gemini-omni-1.1-flash-preview display name from
    Gemini Omni 1.2 Flash to Gemini Omni 1.1 Flash
  * Add the prompt-expansion-mode and enable-safety-checker
    parameters and a 768p output size, used by MiniMax H3 Max
- Re-vendor for the new release: the dependency set is bit-identical
  to 2.70.0, so node_modules.obscpio, node_modules.spec.inc and
  node_modules.sums are unchanged and the licence set of the 71
  vendored packages still holds

-------------------------------------------------------------------
Sat Aug 29 07:24:44 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 2.70.0:
  * Refresh the model catalogue through the inlined @picsart/ai-sdk
    5.10.0 -> 5.21.0
  * Add the creativity, refer-type, turbo-mode and upscale-factor
    flags, and a 360p output size
  * Drop the image-reference, style-reference-urls, omni-image-list
    and omni-video-list flags from the parameter surface
  * Reclassify Runway gen4-ref from image-to-video to image-to-image
    and replace the Pika scenes/frames entries with Pixverse fusion
    in the multi-image flow
- Re-vendor the runtime dependencies for the new release: the seven
  @inquirer/* packages move up a patch/minor level (core 12.0.0 ->
  12.0.1, type 4.0.7 -> 4.1.0, checkbox 5.2.2 -> 5.2.3, confirm
  6.2.0 -> 6.3.0, input 5.1.3 -> 5.1.4, search 4.3.0 -> 4.3.1,
  select 5.2.2 -> 5.2.3); nothing was added or removed, and the
  licence set of the 71 vendored packages is unchanged

-------------------------------------------------------------------
Mon Aug 24 20:12:35 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 2.69.0:
  * Report the generation-tracking events under event_type instead
    of event, so the started and completed events match the current
    telemetry schema
- Re-vendor the runtime dependencies for the new release: picomatch
  4.0.5 -> 4.0.7 is the only change, the dependency set is otherwise
  unchanged

-------------------------------------------------------------------
Sat Aug 22 08:05:18 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Update to version 2.68.1:
  * Update the inlined @picsart/ai-sdk to 5.x so edit, enhance,
    upscale, TTS and related flows match the new catalog (they
    selected no models after the SDK 5 shape change) and hide
    deprecated models
  * Fix a crash after generation when xdg-open is missing, Ctrl+C
    cancel handling, prompt-box corruption, wide-character widths
    and ANSI sequences leaking into piped stdout
  * Harden login and token refresh (timeouts, atomic writes, CI
    PICSART_ACCESS_TOKEN, 401 retry) and reject directory uploads
  * Sanitize untrusted API and model names so they cannot inject
    terminal escape sequences
  * Wire video-extend array file slots, keep edit-mode parameters
    and expand ~ in downloadDir
  * Honour config defaultModel and imagePreview

-------------------------------------------------------------------
Thu Aug 13 11:15:00 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Rename the package from gen-ai to picsart-gen-ai, following a
  review comment on the Factory submission: the tool is a client for
  one specific hosted service and should not hold the generic name
  * The installed command stays /usr/bin/gen-ai, which is what
    upstream's documentation and npx invocations use; only the RPM
    name is qualified with the service
  * Obsolete gen-ai up to 2.60.1, the only version it ever shipped, so
    a copy installed from the devel project is replaced instead of
    co-installed; the bound is frozen rather than tracking the version,
    and the generic name is deliberately not provided back, since
    either would keep laying claim to a name this rename gives up
  * Introduce an npm_name macro for the upstream tarball, the nodejs
    module directory and the command, all of which keep the upstream
    name now that the package name differs from it

-------------------------------------------------------------------
Mon Aug 10 18:03:30 UTC 2026 - Martin Pluskal <mpluskal@suse.com>

- Initial package (version 2.60.1):
  * Command line client for Picsart's generative AI service, which
    creates and edits images, video and audio from the terminal
  * Interactive picker for models and their parameters, batch re-runs
    of failed jobs and a browsable history of past generations
  * Requires a Picsart account; nothing is generated locally
- Package the published npm bundle rather than the git tree: upstream
  inlines two dependencies from a private registry that cannot be
  resolved downstream, so the git tree does not build outside Picsart
- Vendor the runtime dependencies with the node_modules OBS service:
  package-lock.json is the input, node_modules.obscpio the offline
  tarball set consumed by local-npm-registry at build time
