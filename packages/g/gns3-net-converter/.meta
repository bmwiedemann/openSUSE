<package name="gns3-net-converter" project="openSUSE:Factory">
  <title>Official fork by GNS3 team of the gns3 converter.Convert old ini-style GNS3 topologies (&lt;=0.8.7) to the newer version 1+ JSON format</title>
  <description>GNS3 Converter is designed to convert old ini-style GNS3 topologies (&lt;=0.8.7) to the newer version v1+ JSON format for use in GNS3 v1+

The converter will convert all IOS, Cloud and VirtualBox devices to the new format. It will also convert all QEMU based devices (QEMU VM, ASA, PIX, JUNOS &amp; IDS). VPCS nodes will be converted to cloud devices due to lack of information the 0.8.7 topology files.

For topologies containing snapshots, the snapshots will also be converted to the new format automatically.

Documentation

Current documentation for gns3-converter can be found here:
http://gns3-converter.readthedocs.org/en/latest/</description>
  <devel project="Education" package="gns3-net-converter"/>
</package>
