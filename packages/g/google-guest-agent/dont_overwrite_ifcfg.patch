diff -Nru guest-agent-20240213.00.orig/google_guest_agent/network/manager/manager.go guest-agent-20240213.00/google_guest_agent/network/manager/manager.go
--- guest-agent-20240213.00.orig/google_guest_agent/network/manager/manager.go	2024-02-12 22:00:04.000000000 +0100
+++ guest-agent-20240213.00/google_guest_agent/network/manager/manager.go	2024-02-26 14:05:33.128727886 +0100
@@ -300,7 +300,12 @@
 		logger.Debugf("write enabling ifcfg-%s config", iface)
 
 		var ifcfg *os.File
-		ifcfg, err = os.Create("/etc/sysconfig/network/ifcfg-" + iface)
+		filename := "/etc/sysconfig/network/ifcfg-" + iface
+		_, err = os.Stat(filename)
+		if ! os.IsNotExist(err) {
+			return nil
+		}
+		ifcfg, err = os.Create(filename)
 		if err != nil {
 			return err
 		}
