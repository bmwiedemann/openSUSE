<package name="git-credential-oauth" project="openSUSE:Factory">
  <title>git-credential-oauth</title>
  <description>No more passwords! No more personal access tokens! No more SSH keys!

git-credential-oauth is a Git credential helper that securely authenticates to GitHub, GitLab, BitBucket and Gerrit using OAuth.

The first time you authenticate, the helper opens a browser window to the host. Subsequent authentication within storage lifetime is non interactive.</description>
  <devel project="devel:tools:scm" package="git-credential-oauth"/>
</package>
