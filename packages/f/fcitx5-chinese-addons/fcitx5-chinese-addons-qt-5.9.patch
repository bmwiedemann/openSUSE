Index: fcitx5-chinese-addons-5.0.4/gui/pinyindictmanager/pinyindictmanager.cpp
===================================================================
--- fcitx5-chinese-addons-5.0.4.orig/gui/pinyindictmanager/pinyindictmanager.cpp
+++ fcitx5-chinese-addons-5.0.4/gui/pinyindictmanager/pinyindictmanager.cpp
@@ -15,6 +15,8 @@
 #include <QMenu>
 #include <QMessageBox>
 #include <QTemporaryFile>
+#include <QtGlobal>
+#include <QTimer>
 #include <fcitx-utils/i18n.h>
 #include <fcitx-utils/standardpath.h>
 
@@ -77,14 +79,20 @@ PinyinDictManager::PinyinDictManager(QWi
 void PinyinDictManager::load() {}
 
 void PinyinDictManager::save() {
+    auto lam = [this]() {
+                   model_->save();
+                   Q_EMIT changed(false);
+                   Q_EMIT saveFinished();
+               };
+
+#if (QT_VERSION < QT_VERSION_CHECK(5,10,0))
+    QTimer::singleShot(0, this, SLOT(lam()));
+#else{
     QMetaObject::invokeMethod(
         this,
-        [this]() {
-            model_->save();
-            Q_EMIT changed(false);
-            Q_EMIT saveFinished();
-        },
+        lam,
         Qt::QueuedConnection);
+#endif
 }
 
 QString PinyinDictManager::title() { return _("Pinyin dictionary manager"); }
