This patch contains all changes in the 5.0 branche between v5.0.0 and 32a92121f6cd4610a2d56cdf25d410927053de25






diff --git a/.gitignore b/.gitignore
index 58d89172e..05c303247 100644
--- a/.gitignore
+++ b/.gitignore
@@ -23,6 +23,9 @@ changehat/mod_apparmor/pod2htmd.tmp
 changehat/pam_apparmor/get_options.o
 changehat/pam_apparmor/pam_apparmor.o
 changehat/pam_apparmor/pam_apparmor.so
+init/aa-teardown.8
+init/aa-teardown.8.html
+init/pod2htmd.tmp
 parser/po/*.mo
 parser/af_names.h
 parser/cap_names.h
@@ -210,6 +213,7 @@ utils/test/common_test.pyc
 utils/test/.coverage
 utils/test/coverage-report.txt
 utils/test/htmlcov/
+utils/test/pod2htmd.tmp
 utils/vim/apparmor.vim
 utils/vim/apparmor.vim.5
 utils/vim/apparmor.vim.5.html
diff --git a/init/apparmor.systemd b/init/apparmor.systemd
index ccdad3b49..ada6d3867 100644
--- a/init/apparmor.systemd
+++ b/init/apparmor.systemd
@@ -18,7 +18,7 @@
 APPARMOR_FUNCTIONS=/lib/apparmor/rc.apparmor.functions
 
 # This function is used in rc.apparmor.functions
-# shellcheck disable=SC2317
+# shellcheck disable=SC2317,SC2329
 aa_action()
 {
     echo "$1"
@@ -28,7 +28,7 @@ aa_action()
 }
 
 # This function is used in rc.apparmor.functions
-# shellcheck disable=SC2317
+# shellcheck disable=SC2317,SC2329
 aa_log_warning_msg()
 {
     echo "Warning: $*"
@@ -42,14 +42,14 @@ aa_log_failure_msg()
 }
 
 # This function is used in rc.apparmor.functions
-# shellcheck disable=SC2317
+# shellcheck disable=SC2317,SC2329
 aa_log_action_start()
 {
     echo "$@"
 }
 
 # This function is used in rc.apparmor.functions
-# shellcheck disable=SC2317
+# shellcheck disable=SC2317,SC2329
 aa_log_action_end()
 {
     printf ""
@@ -63,7 +63,7 @@ aa_log_daemon_msg()
 }
 
 # This function is used in rc.apparmor.functions
-# shellcheck disable=SC2317
+# shellcheck disable=SC2317,SC2329
 aa_log_skipped_msg()
 {
     echo "Skipped: $*"
diff --git a/init/rc.apparmor.slackware b/init/rc.apparmor.slackware
index d045fc531..cc7dee996 100644
--- a/init/rc.apparmor.slackware
+++ b/init/rc.apparmor.slackware
@@ -18,8 +18,8 @@
 # ----------------------------------------------------------------------
 # rc.apparmor by Steve Beattie
 
-# shellcheck disable=SC2317  # Don't warn about unreachable commands in this file
-#                              since most functions get called via rc.apparmor.functions
+# shellcheck disable=SC2317,SC2329  # Don't warn about unreachable commands in this file
+#                                     since most functions get called via rc.apparmor.functions
 
 # /etc/rc.d/rc.apparmor
 APPARMOR_FUNCTIONS=/lib/apparmor/rc.apparmor.functions
diff --git a/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.err b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.err
new file mode 100644
index 000000000..e69de29bb
diff --git a/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.in b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.in
new file mode 100644
index 000000000..08575f579
--- /dev/null
+++ b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.in
@@ -0,0 +1 @@
+type=AVC msg=audit(1750869214.285:124880): apparmor="ALLOWED" operation="change_profile" class="file" profile="/usr/sbin/php-fpm8.4" name="/usr/sbin/php-fpm8.4//ttrss" pid=869293 comm="php-fpm8.4" target="/usr/sbin/php-fpm8.4//ttrss"
diff --git a/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.out b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.out
new file mode 100644
index 000000000..69be09092
--- /dev/null
+++ b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.out
@@ -0,0 +1,13 @@
+START
+File: testcase_changeprofile_03.in
+Event type: AA_RECORD_ALLOWED
+Audit ID: 1750869214.285:124880
+Operation: change_profile
+Profile: /usr/sbin/php-fpm8.4
+Name: /usr/sbin/php-fpm8.4//ttrss
+Command: php-fpm8.4
+Name2: /usr/sbin/php-fpm8.4//ttrss
+PID: 869293
+Class: file
+Epoch: 1750869214
+Audit subid: 124880
diff --git a/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.profile b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.profile
new file mode 100644
index 000000000..42a3d2b0f
--- /dev/null
+++ b/libraries/libapparmor/testsuite/test_multi/testcase_changeprofile_03.profile
@@ -0,0 +1,4 @@
+/usr/sbin/php-fpm8.4 {
+  change_profile -> /usr/sbin/php-fpm8.4//ttrss,
+
+}
diff --git a/parser/cond_expr.cc b/parser/cond_expr.cc
index 74029fb80..c74b1a44e 100644
--- a/parser/cond_expr.cc
+++ b/parser/cond_expr.cc
@@ -65,7 +65,7 @@ cond_expr::cond_expr(const char *var, cond_op op)
 */
 std::set<std::string> cond_expr::get_set(const char *var)
 {
-	char *var_name = variable::process_var(var);
+	char *var_name = variable::process_var(var, true);
 	if (!var_name) {
 		/* not a variable */
 		return {var};
diff --git a/parser/network.cc b/parser/network.cc
index 4b9f4ee64..aa4efcbbe 100644
--- a/parser/network.cc
+++ b/parser/network.cc
@@ -1129,6 +1129,10 @@ int network_rule::gen_policy_re(Profile &prof)
 		if (!gen_net_rule(prof, netclass, family, type, protocol,
 				  false))
 			goto fail;
+
+		if (skb && !gen_net_rule(prof, AA_CLASS_NETV9_SKB, family,
+					 type, protocol, skb))
+			goto fail;
 	}
 	return RULE_OK;
 
diff --git a/parser/variable.cc b/parser/variable.cc
index 15b974591..247772adf 100644
--- a/parser/variable.cc
+++ b/parser/variable.cc
@@ -123,20 +123,26 @@ ret:
 	return result;
 }
 
-char *variable::process_var(const char *var)
+char *variable::process_var(const char *var, bool quiet)
 {
 	const char *orig;
 	const char *info;
 	int len;
 
 	if (is_var(var, &orig, &len, &info) != VAR_VALID) {
-		PERROR(info, var);
+		if (!quiet)
+			PERROR(info, var);
 		return NULL;
 	}
 
 	return strndup(orig, len);
 }
 
+char *variable::process_var(const char *var)
+{
+	return variable::process_var(var, false);
+}
+
 int variable::add_set_value(struct value_list *values)
 {
 	struct value_list *entry = NULL;
diff --git a/parser/variable.h b/parser/variable.h
index 977194666..922c9cdb8 100644
--- a/parser/variable.h
+++ b/parser/variable.h
@@ -51,6 +51,7 @@ public:
 
 	/* strip off surrounding delimiters around variables */
 	static char *process_var(const char *var);
+	static char *process_var(const char *var, bool quiet);
 };
 
 std::tuple<std::string, std::string, std::string> extract_variable(const std::string& input);
diff --git a/profiles/apparmor.d/abstractions/audio b/profiles/apparmor.d/abstractions/audio
index 56a30afd5..9181121ad 100644
--- a/profiles/apparmor.d/abstractions/audio
+++ b/profiles/apparmor.d/abstractions/audio
@@ -88,6 +88,7 @@ owner @{HOME}/.local/share/openal/hrtf/{,**} r,
 
 # pipewire
 /usr/share/pipewire/client{,-rt}.conf r,
+/usr/share/pipewire/client.conf.d/{,*.conf} r,
 
   # Include additions to the abstraction
   include if exists <abstractions/audio.d>
diff --git a/profiles/apparmor.d/abstractions/dovecot-common b/profiles/apparmor.d/abstractions/dovecot-common
index 729e37331..b9b6c42c8 100644
--- a/profiles/apparmor.d/abstractions/dovecot-common
+++ b/profiles/apparmor.d/abstractions/dovecot-common
@@ -19,6 +19,7 @@
   signal receive peer=dovecot,
 
   owner @{run}/dovecot/config rw,
+  @{run}/dovecot/stats-writer rw,
   owner @{run}/dovecot/dovecot.conf.binary r,
   owner @{run}/dovecot/dovecot.conf.binary.* r,
   owner /tmp/doveconf.* r,
diff --git a/profiles/apparmor.d/abstractions/nameservice b/profiles/apparmor.d/abstractions/nameservice
index e2ade8849..2a4d8be5e 100644
--- a/profiles/apparmor.d/abstractions/nameservice
+++ b/profiles/apparmor.d/abstractions/nameservice
@@ -24,9 +24,6 @@
   /var/lib/sss/mc/passwd  r,
   /var/lib/sss/pipes/nss  rw,
 
-  # On systems where /etc/resolv.conf is managed programmatically, it is
-  # a symlink to @{run}/(whatever program is managing it)/resolv.conf.
-  @{run}/{NetworkManager,connman,netconfig}/resolv.conf r,
   @{etc_ro}/resolvconf/run/resolv.conf  r,
   /mnt/wsl/resolv.conf r,
 
diff --git a/profiles/apparmor.d/abstractions/nameservice-strict b/profiles/apparmor.d/abstractions/nameservice-strict
index 5d073e8fb..2595fdfe9 100644
--- a/profiles/apparmor.d/abstractions/nameservice-strict
+++ b/profiles/apparmor.d/abstractions/nameservice-strict
@@ -31,7 +31,9 @@
   /var/lib/nscd/passwd r,
 
   @{run}/nscd/db* r,
-  @{run}/resolvconf/resolv.conf r,
+  # On systems where /etc/resolv.conf is managed programmatically, it is
+  # a symlink to @{run}/(whatever program is managing it)/resolv.conf.
+  @{run}/{NetworkManager,connman,netconfig,resolvconf}/resolv.conf r,
   @{run}/systemd/resolve/resolv.conf r,
   @{run}/systemd/resolve/stub-resolv.conf r,
 
diff --git a/profiles/apparmor.d/abstractions/postfix-common b/profiles/apparmor.d/abstractions/postfix-common
index d1498c9a8..b3fe784f0 100644
--- a/profiles/apparmor.d/abstractions/postfix-common
+++ b/profiles/apparmor.d/abstractions/postfix-common
@@ -2,7 +2,7 @@
 #
 #    Copyright (C) 2002-2005 Novell/SUSE
 #    Copyright (C) 2015-2018 Canonical, Ltd.
-#    Copyright (C) 2020-2021 Christian Boltz
+#    Copyright (C) 2020-2026 Christian Boltz
 #
 #    This program is free software; you can redistribute it and/or
 #    modify it under the terms of version 2 of the GNU General Public
@@ -23,6 +23,7 @@
 
   unix (send, receive) peer=(label=postfix-master),
 
+  /                     r,
   /etc/mailname         r,
   /etc/postfix/*.cf     r,
   /etc/postfix/*.db     rk,
diff --git a/profiles/apparmor.d/alsamixer b/profiles/apparmor.d/alsamixer
index cd9d8e6d6..45ea6dc5c 100644
--- a/profiles/apparmor.d/alsamixer
+++ b/profiles/apparmor.d/alsamixer
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile alsamixer /{usr,}/bin/alsamixer {
+profile alsamixer /{usr,}/bin/alsamixer flags=(attach_disconnected.path=/att/alsamixer/) {
     include <abstractions/base>
     include <abstractions/consoles>
     include <abstractions/nameservice-strict>
@@ -18,6 +18,7 @@ profile alsamixer /{usr,}/bin/alsamixer {
 
     # pipewire configs
     /usr/share/pipewire/*.conf r,
+    /usr/share/pipewire/client.conf.d/{,*.conf} r,
     # pulseaudio configs
     @{etc_ro}/pulse/*.conf r,
     @{etc_ro}/pulse/*.conf.d/ r,
@@ -27,6 +28,7 @@ profile alsamixer /{usr,}/bin/alsamixer {
     /usr/share/alsa/**/*.conf r,
     @{etc_ro}/alsa/conf.d/ r,
     @{etc_ro}/alsa/conf.d/*.conf r,
+    @{etc_ro}/alsa/conf.d/*.conf.example r,
     # alsa info files
     @{PROC}/asound/** r,
 
@@ -38,6 +40,8 @@ profile alsamixer /{usr,}/bin/alsamixer {
     /dev/snd/by-id/ r,
     /dev/snd/by-path/ r,
 
+    @{sys}/devices/virtual/dmi/id/{bios_vendor,board_vendor,product_name} r,
+
     # PulseAudio communication channels
     owner @{HOME}/.config/pulse/cookie rk,
     owner /run/user/@{uid}/pulse/ r,
@@ -45,5 +49,7 @@ profile alsamixer /{usr,}/bin/alsamixer {
     # Pipewire communication channels
     owner /run/user/@{uid}/pipewire-[0-9] rw,
 
+    /att/alsamixer/systemd/journal/socket w,
+
     include if exists <local/alsamixer>
 }
diff --git a/profiles/apparmor.d/dig b/profiles/apparmor.d/dig
index 4607bc1fd..33fea4f25 100644
--- a/profiles/apparmor.d/dig
+++ b/profiles/apparmor.d/dig
@@ -43,7 +43,9 @@ profile dig /usr/bin/dig {
   ## denied by private-files-strict
   priority=1 owner r @{HOME}/.digrc,
 
+  owner @{PROC}/@{pid}/task/@{tid}/comm rw,
+  @{PROC}/sys/net/ipv4/ip_local_port_range r,
+
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/dig>
 }
-
diff --git a/profiles/apparmor.d/fusermount3 b/profiles/apparmor.d/fusermount3
index 71995f24b..402eadba8 100644
--- a/profiles/apparmor.d/fusermount3
+++ b/profiles/apparmor.d/fusermount3
@@ -55,6 +55,8 @@ profile fusermount3 /usr/bin/fusermount3 {
   @{run}/mount/utab.lock rwk,
 
   @{etc_ro}/fuse.conf r,
+  @{etc_ro}/fuse3.conf r,
+
   @{PROC}/@{pid}/{mounts,mountinfo} r,
 
   @{exec_path} mr,
diff --git a/profiles/apparmor.d/lsblk b/profiles/apparmor.d/lsblk
index 1f4587528..d5ac7f155 100644
--- a/profiles/apparmor.d/lsblk
+++ b/profiles/apparmor.d/lsblk
@@ -27,6 +27,12 @@ profile lsblk /usr/bin/lsblk {
   @{sys}/devices/virtual/** r,
   @{sys}/devices/platform/** r,
 
+  # needed for --fs (show filesystem information)
+  deny / r, # no idea why this would be needed (only seen for --fs)
+  /dev/nvme* r,
+  /dev/dm-* r,
+  /dev/sd* r,
+
   # Needed for Xen PVH guests
   @{sys}/devices/vbd-@{int}/block/** r,
 
diff --git a/profiles/apparmor.d/lsof b/profiles/apparmor.d/lsof
index 85b7bcf87..9efaa52cc 100644
--- a/profiles/apparmor.d/lsof
+++ b/profiles/apparmor.d/lsof
@@ -36,6 +36,7 @@ profile lsof /usr/bin/lsof flags=(attach_disconnected.path=/att/lsof/) {
 
   @{PROC} r,
   @{PROC}/locks r,
+  @{PROC}/@{pids}/attr/current r,
   @{PROC}/@{pids}/fd/ r,
   @{PROC}/@{pids}/fdinfo/* r,
   @{PROC}/@{pids}/lock r,
diff --git a/profiles/apparmor.d/lsusb b/profiles/apparmor.d/lsusb
index c666608c5..a32d82e91 100644
--- a/profiles/apparmor.d/lsusb
+++ b/profiles/apparmor.d/lsusb
@@ -15,6 +15,7 @@ include <tunables/global>
 
 profile lsusb /usr/bin/lsusb {
   include <abstractions/base>
+  include <abstractions/consoles>
 
   /usr/bin/lsusb mr,
 
@@ -43,8 +44,11 @@ profile lsusb /usr/bin/lsusb {
   # can silence the denial.
   deny capability sys_admin,
 
+  /etc/udev/hwdb.bin r,
+
   /dev/bus/usb/@{d}@{d}@{d}/@{d}@{d}@{d} rw,
 
+  / r,
   /dev/ r,
   /dev/bus/usb/ r,
   @{run}/udev/data/*usb:* r,
@@ -54,6 +58,9 @@ profile lsusb /usr/bin/lsusb {
   @{sys}/bus/usb/devices/ r,
   @{sys}/class/ r,
 
+  @{sys}/devices/@{pci_bus}/uevent r,
+  @{sys}/devices/@{pci_bus}/**/uevent r,
+
   @{sys}/devices/**/usb[0-9]**/uevent r,
   @{sys}/devices/**/usb[0-9]**/busnum r,
   @{sys}/devices/**/usb[0-9]**/descriptors r,
diff --git a/profiles/apparmor.d/php-fpm b/profiles/apparmor.d/php-fpm
index 7253e8bc0..d3ebd94ac 100644
--- a/profiles/apparmor.d/php-fpm
+++ b/profiles/apparmor.d/php-fpm
@@ -38,6 +38,7 @@ profile php-fpm /usr/{bin,sbin}/php-fpm* flags=(attach_disconnected) {
 
   # LP: #2061113
   owner @{run}/systemd/notify w,
+  /systemd/notify w,  # disconnected path
 
   # to reload
   @{exec_path} rix,
diff --git a/profiles/apparmor.d/plasmashell b/profiles/apparmor.d/plasmashell
index dd0049497..4df823ac6 100644
--- a/profiles/apparmor.d/plasmashell
+++ b/profiles/apparmor.d/plasmashell
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile plasmashell /usr/bin/plasmashell {
+profile plasmashell /usr/bin/plasmashell flags=(attach_disconnected.path=/att/plasmashell/) {
   include <abstractions/dbus-session>
 
   capability,
@@ -26,6 +26,9 @@ profile plasmashell /usr/bin/plasmashell {
   /{,**} mrwlk,
   @{exec_path} mr,
 
+  # even if it's covered by the very generous rules above... (to document the disconnected path)
+  /att/plasmashell/systemd/journal/socket w,
+
   profile QtWebEngineProcess {
     capability,
     userns,
diff --git a/profiles/apparmor.d/proftpd b/profiles/apparmor.d/proftpd
index 54a4df419..75ff2bcb0 100644
--- a/profiles/apparmor.d/proftpd
+++ b/profiles/apparmor.d/proftpd
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile proftpd /usr/sbin/proftpd {
+profile proftpd /usr/sbin/proftpd flags=(attach_disconnected.path=/att/proftpd/) {
   include <abstractions/base>
   include <abstractions/nameservice>
   include <abstractions/user-tmp>
@@ -19,7 +19,7 @@ profile proftpd /usr/sbin/proftpd {
 
   mqueue getattr type=posix /,
   mqueue read type=posix /,
-
+  / r,
 
   # Configuration files
   /etc/ftpusers r,
@@ -28,18 +28,20 @@ profile proftpd /usr/sbin/proftpd {
 
   # User/Group Records
   /run/systemd/userdb/ r,
-  /run/systemd/userdb/io.systemd.DynamicUser rw,
+  /{att/proftpd/,}run/systemd/userdb/io.systemd.DynamicUser rw,
+  /att/proftpd/run/systemd/userdb/io.systemd.Machine rw,
 
   # ProFTPD logs
   /var/log/proftpd/** rw,
   owner /etc/proftpd/** rw,
+  /att/proftpd/systemd/journal/dev-log w,
 
   # Process info
   /run/proftpd.delay rwk,
   /run/proftpd.pid rwk,
   /run/proftpd.scoreboard rwk,
   /run/proftpd.scoreboard.lck rwk,
-  owner /run/proftpd/* rw,
+  /run/proftpd/* rwk,
   owner /run/test.sock w,
   owner /var/log/xferlog w,
 
diff --git a/profiles/apparmor.d/samba-bgqd b/profiles/apparmor.d/samba-bgqd
index e77bf7bdc..db82135af 100644
--- a/profiles/apparmor.d/samba-bgqd
+++ b/profiles/apparmor.d/samba-bgqd
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile samba-bgqd /usr/lib*/samba/{,samba/}samba-bgqd {
+profile samba-bgqd /usr/lib*/samba/{,samba/}samba-bgqd flags=(attach_disconnected.path=/att/samba-bgqd/) {
   include <abstractions/base>
   include <abstractions/cups-client>
   include <abstractions/nameservice>
@@ -18,6 +18,8 @@ profile samba-bgqd /usr/lib*/samba/{,samba/}samba-bgqd {
   @{exec_path} mr,
   /var/cache/samba/printing/*.tdb rwk,
 
+  /att/samba-bgqd/systemd/notify w,
+
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/samba-bgqd>
 }
diff --git a/profiles/apparmor.d/sbin.syslog-ng b/profiles/apparmor.d/sbin.syslog-ng
index 36451cbff..76620384b 100644
--- a/profiles/apparmor.d/sbin.syslog-ng
+++ b/profiles/apparmor.d/sbin.syslog-ng
@@ -55,8 +55,8 @@ profile syslog-ng /{usr/,}{bin,sbin}/syslog-ng {
   @{CHROOT_BASE}/var/lib/*/dev/log w,
   @{CHROOT_BASE}/var/lib/syslog-ng/syslog-ng.persist* rw,
   @{CHROOT_BASE}/var/log/** w,
-  @{CHROOT_BASE}/@{run}/syslog-ng.pid krw,
-  @{CHROOT_BASE}/@{run}/syslog-ng.ctl rw,
+  @{CHROOT_BASE}@{run}/syslog-ng.pid krw,
+  @{CHROOT_BASE}@{run}/syslog-ng.ctl rw,
   /{var,var/run,run}/log/journal/ r,
   /{var,var/run,run}/log/journal/*/ r,
   /{var,var/run,run}/log/journal/*/*.journal r,
diff --git a/profiles/apparmor.d/transmission b/profiles/apparmor.d/transmission
index cbe5593a9..1f6d20e0f 100644
--- a/profiles/apparmor.d/transmission
+++ b/profiles/apparmor.d/transmission
@@ -22,6 +22,7 @@ profile transmission-daemon /usr/bin/transmission-daemon flags=(attach_disconnec
   owner @{PROC}/@{pid}/mounts r,
   @{PROC}/sys/kernel/random/uuid r,
 
+  /systemd/notify w,  # disconnected path
   @{run}/systemd/notify w,
 
   /etc/transmission-daemon/** r,
@@ -32,9 +33,11 @@ profile transmission-daemon /usr/bin/transmission-daemon flags=(attach_disconnec
   /usr/share/transmission/web/** r,
   /usr/share/transmission/public_html/** r,
 
-  owner /var/lib/transmission-daemon/.config/transmission-daemon/** rw,
-  owner /var/lib/transmission-daemon/downloads/** rw,
-  owner /var/lib/transmission-daemon/info/**      rw,
+  owner /var/lib/transmission{-daemon,}/.config/ rw,
+  owner /var/lib/transmission{-daemon,}/.config/transmission-daemon/ rw,
+  owner /var/lib/transmission{-daemon,}/.config/transmission-daemon/** rw,
+  owner /var/lib/transmission{-daemon,}/downloads/** rw,
+  owner /var/lib/transmission{-daemon,}/info/**      rw,
 
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/transmission>
diff --git a/profiles/apparmor.d/usr.lib.dovecot.dict b/profiles/apparmor.d/usr.lib.dovecot.dict
index 4ccf8d23d..8bad1590d 100644
--- a/profiles/apparmor.d/usr.lib.dovecot.dict
+++ b/profiles/apparmor.d/usr.lib.dovecot.dict
@@ -27,6 +27,7 @@ profile dovecot-dict /usr/lib*/dovecot/dict {
   /etc/dovecot/dovecot-dict-sql.conf.ext r,
   /etc/my.cnf r,
   @{exec_path} mr,
+  @{run}/dovecot/dict rw,
 
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/usr.lib.dovecot.dict>
diff --git a/profiles/apparmor.d/usr.lib.dovecot.lmtp b/profiles/apparmor.d/usr.lib.dovecot.lmtp
index 8d7ff065a..d95ab4414 100644
--- a/profiles/apparmor.d/usr.lib.dovecot.lmtp
+++ b/profiles/apparmor.d/usr.lib.dovecot.lmtp
@@ -35,7 +35,9 @@ profile dovecot-lmtp /usr/lib*/dovecot/lmtp {
   @{PROC}/*/mounts r,
   /tmp/dovecot.lmtp.* rw,
   @{exec_path} mr,
+  @{run}/dovecot/auth-userdb rw,
   @{run}/dovecot/mounts r,
+  /var/spool/postfix/private/dovecot-lmtp rw,
 
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/usr.lib.dovecot.lmtp>
diff --git a/profiles/apparmor.d/usr.sbin.avahi-daemon b/profiles/apparmor.d/usr.sbin.avahi-daemon
index fb768aff5..66b4e12c5 100644
--- a/profiles/apparmor.d/usr.sbin.avahi-daemon
+++ b/profiles/apparmor.d/usr.sbin.avahi-daemon
@@ -33,6 +33,9 @@ profile avahi-daemon /usr/{bin,sbin}/avahi-daemon flags=(attach_disconnected) {
   @{run}/avahi-daemon/socket w,
   @{run}/systemd/notify w,
 
+  /systemd/journal/dev-log w, # disconnected path
+  /systemd/notify w, # disconnected path
+
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/usr.sbin.avahi-daemon>
 }
diff --git a/profiles/apparmor.d/usr.sbin.dovecot b/profiles/apparmor.d/usr.sbin.dovecot
index d1bbbdb56..12f441a90 100644
--- a/profiles/apparmor.d/usr.sbin.dovecot
+++ b/profiles/apparmor.d/usr.sbin.dovecot
@@ -39,6 +39,7 @@ profile dovecot /usr/{bin,sbin}/dovecot flags=(attach_disconnected) {
   unix (receive, send) type=stream peer=(label=/usr/lib*/dovecot/anvil),
   unix (receive, send) type=stream peer=(label=dovecot-anvil),
 
+  / r,
   /etc/dovecot/** r,
   /etc/mtab r,
   /etc/lsb-release r,
diff --git a/profiles/apparmor.d/usr.sbin.nmbd b/profiles/apparmor.d/usr.sbin.nmbd
index 765f79175..b682a670d 100644
--- a/profiles/apparmor.d/usr.sbin.nmbd
+++ b/profiles/apparmor.d/usr.sbin.nmbd
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile nmbd /usr/{bin,sbin}/nmbd {
+profile nmbd /usr/{bin,sbin}/nmbd flags=(attach_disconnected.path=/att/nmbd/) {
   include <abstractions/base>
   include <abstractions/nameservice>
   include <abstractions/samba>
@@ -28,6 +28,8 @@ profile nmbd /usr/{bin,sbin}/nmbd {
   @{run}/nmbd.pid rwk,
   @{run}/samba/** rwk,
   @{run}/systemd/notify w,
+  /att/nmbd/systemd/notify w,
+  /att/nmbd/systemd/journal/dev-log w,
 
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/usr.sbin.nmbd>
diff --git a/profiles/apparmor.d/usr.sbin.smbd b/profiles/apparmor.d/usr.sbin.smbd
index b54b4f551..3b0164a5f 100644
--- a/profiles/apparmor.d/usr.sbin.smbd
+++ b/profiles/apparmor.d/usr.sbin.smbd
@@ -2,7 +2,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile smbd /usr/{bin,sbin}/smbd {
+profile smbd /usr/{bin,sbin}/smbd flags=(attach_disconnected.path=/att/smbd/) {
   include <abstractions/authentication>
   include <abstractions/base>
   include <abstractions/consoles>
@@ -27,6 +27,7 @@ profile smbd /usr/{bin,sbin}/smbd {
 
   signal send set=term peer=samba-bgqd,
 
+  / r,
   /etc/mtab r,
   /etc/netgroup r,
   /etc/printcap r,
@@ -62,6 +63,9 @@ profile smbd /usr/{bin,sbin}/smbd {
   @{HOMEDIRS}/** lrwk,
   /var/lib/samba/usershares/{,**} lrwk,
 
+  /att/smbd/systemd/notify w,
+  /att/smbd/systemd/journal/dev-log w,
+
   # Permissions for all configured shares (file autogenerated by
   # update-apparmor-samba-profile on service startup on Debian and openSUSE)
   include if exists <samba/smbd-shares>
diff --git a/profiles/apparmor.d/wpa_supplicant b/profiles/apparmor.d/wpa_supplicant
index 06c0fc46d..1d6a9c391 100644
--- a/profiles/apparmor.d/wpa_supplicant
+++ b/profiles/apparmor.d/wpa_supplicant
@@ -12,7 +12,7 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-profile wpa_supplicant /usr/sbin/wpa_supplicant {
+profile wpa_supplicant /usr/sbin/wpa_supplicant flags=(attach_disconnected.path=/att/wpa_supplicant/) {
   include <abstractions/base>
   include <abstractions/dbus-strict>
 
@@ -116,6 +116,7 @@ profile wpa_supplicant /usr/sbin/wpa_supplicant {
   @{exec_path} mr,
 
   owner /dev/rfkill r,
+  /etc/libnl/classid r,
   owner /etc/group r,
   owner /etc/nsswitch.conf r,
 
@@ -124,8 +125,12 @@ profile wpa_supplicant /usr/sbin/wpa_supplicant {
   owner @{run}/wpa_supplicant/ w,
   owner @{run}/wpa_supplicant/** rw,
 
+  /att/wpa_supplicant/run/dbus/system_bus_socket rw,
+
   owner @{run}/netplan/* r,
 
+  owner /var/log/wpa_supplicant.log w,
+
   network inet dgram,
   network inet6 dgram,
   network netlink raw,
diff --git a/profiles/apparmor/profiles/extras/usr.sbin.postalias b/profiles/apparmor/profiles/extras/usr.sbin.postalias
index 702625e8b..1aa51d97f 100644
--- a/profiles/apparmor/profiles/extras/usr.sbin.postalias
+++ b/profiles/apparmor/profiles/extras/usr.sbin.postalias
@@ -1,7 +1,7 @@
 # ------------------------------------------------------------------
 #
 #    Copyright (C) 2002-2005 Novell/SUSE
-#    Copyright (C) 2021 Christian Boltz
+#    Copyright (C) 2021-2026 Christian Boltz
 #
 #    This program is free software; you can redistribute it and/or
 #    modify it under the terms of version 2 of the GNU General Public
@@ -13,12 +13,15 @@ abi <abi/5.0>,
 
 include <tunables/global>
 
-/usr/sbin/postalias {
+profile postalias /usr/sbin/postalias flags=(attach_disconnected.path=/att/postalias/) {
   include <abstractions/base>
   include <abstractions/kerberosclient>
   include <abstractions/nameservice>
   include <abstractions/consoles>
   include <abstractions/postfix-common>
+
+  /att/postalias/dev/null              r,
+
   /etc/aliases                         r,
   /etc/aliases.{lm,}db                 rwlk,
   /etc/postfix                         r,
@@ -38,4 +41,5 @@ include <tunables/global>
 
   # Site-specific additions and overrides. See local/README for details.
   include if exists <local/usr.sbin.postalias>
+  include if exists <local/postalias>
 }
diff --git a/utils/aa-notify b/utils/aa-notify
index dec400f91..09472b19b 100755
--- a/utils/aa-notify
+++ b/utils/aa-notify
@@ -809,14 +809,21 @@ def display_notification(ev, rl, message, userns_special_profiles):
         n.add_action('clicked', 'Allow', cb_add_to_profile, (ev, rl, userns_special_profiles))
     n.add_action('more_clicked', 'Show More', cb_more_info, (ev, rl, userns_special_profiles))
 
-    n.show()
+    try:
+        n.show()
+    except DBusException as exc:
+        print(_('ERROR! Failed to display notification:\n    {message}\n{exc}\n').format(message=message.replace('\n', '\n    '), exc=exc), file=sys.stderr)
 
 
 def display_aggregated_notification(rl, aggregated, maximum_number_notification_profiles, keys_to_aggregate, special_profiles):
     notification, summary, more_info, clean_rules = get_aggregated(rl, aggregated, maximum_number_notification_profiles, keys_to_aggregate, special_profiles)
     n = notify2.Notification(_('AppArmor security notice'), notification, 'gtk-dialog-warning')
     n.add_action('more_aggregated_clicked', 'Show More Info', cb_more_info_aggregated, (summary, more_info, clean_rules))
-    n.show()
+
+    try:
+        n.show()
+    except DBusException as exc:
+        print(_('ERROR! Failed to display notification:\n    {message}\n{exc}\n').format(message=more_info.replace('\n', '\n    '), exc=exc), file=sys.stderr)
 
 
 def main():
diff --git a/utils/apparmor/logparser.py b/utils/apparmor/logparser.py
index ec7af7c07..aa680b95e 100644
--- a/utils/apparmor/logparser.py
+++ b/utils/apparmor/logparser.py
@@ -147,6 +147,8 @@ class ReadLog:
             return 'unix'
         elif e['operation'] == 'change_onexec':
             return 'change_profile'
+        elif e['operation'] == 'change_profile':
+            return 'change_profile'
         elif e['class'] == 'file' or self.op_type(e) == 'file':
             return 'file'
         elif e['operation'] == 'capable':
@@ -155,8 +157,6 @@ class ReadLog:
             return 'network'
         elif e['operation'] == 'change_hat':
             return 'change_hat'
-        elif e['operation'] == 'change_profile':
-            return 'change_profile'
         elif e['operation'] == 'ptrace':
             return 'ptrace'
         elif e['operation'] == 'signal':
@@ -350,6 +350,10 @@ class ReadLog:
             UnixRule.hashlog_from_event(self.hashlog[aamode][full_profile]['unix'], e)
             return
 
+        elif e['operation'] == 'change_profile' or e['operation'] == 'change_onexec':
+            ChangeProfileRule.hashlog_from_event(self.hashlog[aamode][full_profile]['change_profile'], e)
+            return
+
         elif e['class'] == 'file' or self.op_type(e) == 'file':
             FileRule.hashlog_from_event(self.hashlog[aamode][full_profile]['file'], e)
 
@@ -368,10 +372,6 @@ class ReadLog:
             self.hashlog[aamode][full_profile]['change_hat'][e['name2']] = True
             return
 
-        elif e['operation'] == 'change_profile' or e['operation'] == 'change_onexec':
-            ChangeProfileRule.hashlog_from_event(self.hashlog[aamode][full_profile]['change_profile'], e)
-            return
-
         elif e['operation'] == 'ptrace':
             if not e['peer']:
                 self.debug_logger.debug('ignored garbage ptrace event with empty peer')
diff --git a/utils/vim/create-apparmor.vim.py b/utils/vim/create-apparmor.vim.py
index c855aae41..d9ae7b4f9 100644
--- a/utils/vim/create-apparmor.vim.py
+++ b/utils/vim/create-apparmor.vim.py
@@ -117,48 +117,54 @@ def my_repl(matchobj):
     return matchobj.group(0)
 
 
-def create_file_rule(highlighting, permissions, comment, denyrule=0):
+def create_file_rule(highlighting, permissions, comment, denyrule=0, transition = 0):
 
     if denyrule == 0:
         keywords = '@@auditdenyowner@@'
     else:
         keywords = '@@audit_DENY_owner@@'  # TODO: not defined yet, will be '(audit\s+)?deny\s+(owner\s+)?'
 
+    if transition:
+        transition = '@@TRANSITION@@'
+    else:
+        transition = ''
+
     sniplet = ''
     sniplet = sniplet + "\n" + '" ' + comment + "\n"
 
-    prefix = r'syn match  ' + highlighting + r' /\v^\s*' + keywords
-    suffix = r'@@EOL@@/ contains=sdGlob,sdComment nextgroup=@sdEntry,sdComment,sdError,sdInclude' + "\n"
-    # filename without quotes
-    sniplet = sniplet + prefix + r'@@FILENAME@@\s+' + permissions + suffix
-    # filename with quotes
-    sniplet = sniplet + prefix + r'"@@FILENAME@@"\s+' + permissions + suffix
-    # filename without quotes, reverse syntax
-    sniplet = sniplet + prefix + permissions + r'\s+@@FILENAME@@' + suffix
-    # filename with quotes, reverse syntax
-    sniplet = sniplet + prefix + permissions + r'\s+"@@FILENAME@@"+' + suffix
+    for file_kw in ['', r'file\s+']:
+        prefix = r'syn match  ' + highlighting + r' /\v^\s*' + file_kw + keywords
+        suffix = r'@@EOL@@/ contains=sdGlob,sdComment nextgroup=@sdEntry,sdComment,sdError,sdInclude' + "\n"
+
+        for fn_quotes in ['', '"']:
+            quoted_filename = fn_quotes + r'@@FILENAME@@' + fn_quotes  # depending on fn_quotes, might also not be quoted
+            # trailing permissions syntax
+            sniplet = sniplet + prefix + quoted_filename + r'\s+' + permissions + transition + suffix
+            # leading permissions
+            sniplet = sniplet + prefix + permissions + r'\s+' + quoted_filename + transition + suffix
 
     return sniplet
 
 
 filerule = ''
-filerule = filerule + create_file_rule('sdEntryWriteExec ', r'(l|r|w|a|m|k|[iuUpPcC]x)+@@TRANSITION@@', 'write + exec/mmap - danger! (known bug: accepts aw to keep things simple)')
-filerule = filerule + create_file_rule('sdEntryUX',  r'(r|m|k|ux|pux)+@@TRANSITION@@',  'ux(mr) - unconfined entry, flag the line red. also includes pux which is unconfined if no profile exists')
-filerule = filerule + create_file_rule('sdEntryUXe', r'(r|m|k|Ux|PUx)+@@TRANSITION@@',  'Ux(mr) and PUx(mr) - like ux + clean environment')
-filerule = filerule + create_file_rule('sdEntryPX',  r'(r|m|k|px|cx|pix|cix)+@@TRANSITION@@',  'px/cx/pix/cix(mrk) - standard exec entry, flag the line blue')
-filerule = filerule + create_file_rule('sdEntryPXe', r'(r|m|k|Px|Cx|Pix|Cix)+@@TRANSITION@@', 'Px/Cx/Pix/Cix(mrk) - like px/cx + clean environment')
+filerule = filerule + create_file_rule('sdEntryWriteExec ', r'[lrwamk]*[iuUpPcC][iUu]?x[lrwamk]*', 'write + exec/mmap - danger! (known bug: accepts aw to keep things simple)', transition=1)
+filerule = filerule + create_file_rule('sdEntryUX',  r'(r|m|k|ux|pux)+',  'ux(mr) - unconfined entry, flag the line red. also includes pux which is unconfined if no profile exists', transition=1)
+filerule = filerule + create_file_rule('sdEntryUXe', r'(r|m|k|Ux|PUx)+',  'Ux(mr) and PUx(mr) - like ux + clean environment', transition=1)
+filerule = filerule + create_file_rule('sdEntryPX',  r'(r|m|k|px|cx|pix|cix)+',  'px/cx/pix/cix(mrk) - standard exec entry, flag the line blue', transition=1)
+filerule = filerule + create_file_rule('sdEntryPXe', r'(r|m|k|Px|Cx|Pix|Cix)+', 'Px/Cx/Pix/Cix(mrk) - like px/cx + clean environment', transition=1)
 filerule = filerule + create_file_rule('sdEntryIX',  r'(r|m|k|ix)+',  'ix(mr) - standard exec entry, flag the line green')
 filerule = filerule + create_file_rule('sdEntryM',   r'(r|m|k)+',  'mr - mmap with PROT_EXEC')
 
-filerule = filerule + create_file_rule('sdEntryM',   r'(r|m|k|x)+',  'special case: deny x is allowed (does not need to be ix, px, ux or cx)', 1)
+filerule = filerule + create_file_rule('sdEntryM',   r'(r|m|k|x)+',  'special case: deny x is allowed (does not need to be ix, px, ux or cx)', denyrule=1)
 # syn match  sdEntryM /@@DENYFILE@@(r|m|k|x)+@@EOL@@/ contains=sdGlob,sdComment nextgroup=@sdEntry,sdComment,sdError,sdInclude
 
 
 filerule = filerule + create_file_rule('sdError',    r'\S*(w\S*a|a\S*w)\S*',  'write + append is an error')
-filerule = filerule + create_file_rule('sdEntryW',   r'(l|r|w|k)+',  'write entry, flag the line yellow')
-filerule = filerule + create_file_rule('sdEntryW',   r'(l|r|a|k)+',  'append entry, flag the line yellow')
-filerule = filerule + create_file_rule('sdEntryK',   r'[rlk]+',  'read entry + locking, currently no highlighting')
-filerule = filerule + create_file_rule('sdEntryR',   r'[rl]+',  'read entry, no highlighting')
+for writeperm in ['a', 'w']:
+    filerule = filerule + create_file_rule('sdEntryW',   r'[r' + writeperm + 'k]*l[r' + writeperm + 'k]*',  'link (and optionally write) entry, flag the line yellow', transition=1)
+    filerule = filerule + create_file_rule('sdEntryW',   r'[r' + writeperm + 'k]+',  'write entry, flag the line yellow')
+filerule = filerule + create_file_rule('sdEntryK',   r'[rk]+',  'read entry + locking, currently no highlighting')
+filerule = filerule + create_file_rule('sdEntryR',   r'r+',  'read entry, no highlighting')
 
 # " special case: deny x is allowed (doesn't need to be ix, px, ux or cx)
 # syn match  sdEntryM /@@DENYFILE@@(r|m|k|x)+@@EOL@@/ contains=sdGlob,sdComment nextgroup=@sdEntry,sdComment,sdError,sdInclude
