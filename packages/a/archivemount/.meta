<package name="archivemount" project="openSUSE:Factory">
  <title>Archivemount</title>
  <description>Archivemount is a piece of glue code between libarchive (http://code.google.com/p/libarchive/) and FUSE (http://fuse.sourceforge.net). It can be used to mount a (possibly compressed) archive (as in .tar.gz or .tar.bz2) and use it like an ordinary filesystem.
</description>
  <devel project="Archiving" package="archivemount"/>
</package>
