diff -Nru aws-checksums-0.2.11.orig/CMakeLists.txt aws-checksums-0.2.11/CMakeLists.txt
--- aws-checksums-0.2.11.orig/CMakeLists.txt	2026-08-18 01:08:11.000000000 +0000
+++ aws-checksums-0.2.11/CMakeLists.txt	2026-09-08 09:15:15.239739205 +0000
@@ -57,6 +57,7 @@
 # We are not ABI stable yet
 # TODO: switch to VERSION derived from the VERSION file (via aws_get_version) once SOVERSION is set explicitly, to avoid changing the SONAME.
 set_target_properties(${PROJECT_NAME} PROPERTIES VERSION 1.0.0)
+set_target_properties(${CMAKE_PROJECT_NAME} PROPERTIES SOVERSION 1)
 
 if (USE_CPU_EXTENSIONS)
     if (AWS_ARCH_INTEL)
