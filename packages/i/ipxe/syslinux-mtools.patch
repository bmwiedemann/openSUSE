diff --git a/src/util/genfsimg b/src/util/genfsimg
index 611d3350a..469edad27 100755
--- a/src/util/genfsimg
+++ b/src/util/genfsimg
@@ -426,7 +426,7 @@ if [ -n "${FATIMG}" ] ; then
     mformat -v iPXE ${FATARGS} F:
     mcopy -s "${FATDIR}"/* F:
     if [ "${BIOSDIR}" = "${FATDIR}" ] ; then
-	syslinux --offset "$(( FATOFFS * 512 ))" "${FATIMG}"
+	syslinux-mtools --offset "$(( FATOFFS * 512 ))" "${FATIMG}"
     fi
 fi
 
