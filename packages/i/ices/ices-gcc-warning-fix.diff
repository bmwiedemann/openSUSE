--- src/resample.c-dist	2007-06-12 11:53:04.000000000 +0200
+++ src/resample.c	2007-06-12 11:53:40.000000000 +0200
@@ -174,7 +174,7 @@ int resampler_init(resampler_state *stat
                 assert(beta > 2.0);
                 break;
             default:
-                assert("arglist" == "valid");
+                /* assert("arglist" == "valid"); */
                 return -1;
             }
             op1 = va_arg(argp, resampler_parameter);
