--- conf/ifplugd.action	2004-03-03 01:02:18.000000000 +0000
+++ conf/ifplugd.action	2006-03-09 10:06:01.000000000 +0000
@@ -24,7 +24,7 @@
 	exit 1
 fi
 
-[ "$2" = "up" ] && exec /sbin/ifup $1
-[ "$2" = "down" ] && exec /sbin/ifdown $1
+[ "$2" = "up" ] && exec /sbin/ifup $1 -o ifplugd
+[ "$2" = "down" ] && exec /sbin/ifdown $1 -o ifplugd
 
 exit 1
