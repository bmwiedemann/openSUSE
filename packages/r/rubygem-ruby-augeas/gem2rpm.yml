:license: LGPL-2.1+
:patches:
  'COPYING.patch': -p1
  gcc14.patch: -p1
:preamble: |-
  BuildRequires: pkgconfig
  BuildRequires: augeas-devel
  %if 0%{?suse_version} == 1110
  BuildRequires: libxml2-devel
  %endif
:main:
  :preamble: |-
    Requires: augeas
