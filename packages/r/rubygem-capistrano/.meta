<package name="rubygem-capistrano" project="openSUSE:Factory">
  <title>Capistrano</title>
  <description>Capistrano is a framework and utility for executing commands in  
parallel on multiple remote machines, via SSH. The primary goal is to  
simplify and automate the deployment of web applications.  
  
A utility for executing commands in parallel on multiple machines, such  
as deploying applications to a cluster of boxes.  </description>
  <devel project="devel:languages:ruby:extensions" package="rubygem-capistrano"/>
  <url>http://github.com/capistrano/capistrano</url>
</package>
