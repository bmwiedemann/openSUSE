--- open-gpu-kernel-modules-615.71.09/kernel-open/nvidia/os-interface.c.orig	2026-09-23 03:49:05.127724664 +0200
+++ open-gpu-kernel-modules-615.71.09/kernel-open/nvidia/os-interface.c	2026-09-23 03:54:14.069735312 +0200
@@ -3028,7 +3028,12 @@ void* NV_API_CALL os_cgroup_parent(void
 #if defined(NV_DMEM_CGROUP_PRESENT)
 void* NV_API_CALL os_dmem_cgroup_register_region(const char *name, NvU64 size, NvU64 precharge, void **prechargePool)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(7, 3, 0)
+    void *region = dmem_cgroup_register_region(
+        &(struct dmem_cgroup_init){ .size = size }, name);
+#else
     void *region = dmem_cgroup_register_region(size, name);
+#endif
     if (IS_ERR(region))
     {
         return NULL;
