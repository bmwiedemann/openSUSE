Index: node-git.3bba40abc6/deps/npm/lib/help-search.js
===================================================================
--- node-git.3bba40abc6.orig/deps/npm/lib/help-search.js
+++ node-git.3bba40abc6/deps/npm/lib/help-search.js
@@ -28,7 +28,7 @@ class HelpSearch extends BaseCommand {
     if (!args.length)
       throw this.usage
 
-    const docPath = path.resolve(__dirname, '..', 'docs/content')
+    const docPath = '/usr/share/doc/packages/nodejs';
 
     const files = await glob(`${docPath}/*/*.md`)
     const data = await this.readFiles(files)
Index: node-git.3bba40abc6/deps/npm/lib/npm.js
===================================================================
--- node-git.3bba40abc6.orig/deps/npm/lib/npm.js
+++ node-git.3bba40abc6/deps/npm/lib/npm.js
@@ -223,7 +223,13 @@ const npm = module.exports = new class e
   }
 
   get globalPrefix () {
-    return this.config.globalPrefix
+    let prefix = this.config.globalPrefix
+
+    // don't poop all over distro territory - use /usr/local instead
+    if (prefix === '/usr')
+      return '/usr/local'
+
+    return prefix;
   }
 
   set globalPrefix (r) {
