Index: deno-2.9.0/Cargo.toml
===================================================================
--- deno-2.9.0.orig/Cargo.toml
+++ deno-2.9.0/Cargo.toml
@@ -476,7 +476,6 @@ winres = "=0.1.12"
 [profile.release]
 codegen-units = 1
 incremental = false
-lto = true
 opt-level = 'z' # Optimize for size
 split-debuginfo = "packed"
 debug = "line-tables-only"
@@ -486,7 +485,6 @@ panic = "abort"
 [profile.bench]
 codegen-units = 1
 incremental = false
-lto = true
 opt-level = 'z' # Optimize for size
 split-debuginfo = "packed"
 debug = "line-tables-only"
@@ -504,7 +502,6 @@ inherits = "release"
 # codegen layout); release-lite is the local iteration profile, so keep it fast.
 incremental = true
 codegen-units = 128
-lto = "thin"
 
 # Key generation is too slow on `debug`
 [profile.dev.package.num-bigint-dig]
