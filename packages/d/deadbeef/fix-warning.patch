--- deadbeef-1.9.2/fastftoi.h.orig	2022-11-03 16:13:27.001073213 +0100
+++ deadbeef-1.9.2/fastftoi.h	2022-11-03 16:18:08.967427385 +0100
@@ -119,7 +119,14 @@
 /* Optimized code path for x86_64 builds. Uses SSE2 intrinsics. This can be
    done safely because all x86_64 CPUs supports SSE2. */
 #if (defined(__FORCE_SSE2__)) || (defined(_MSC_VER) && defined(_WIN64)) || (defined(__GNUC__) && defined (__x86_64__))
-#pragma warning "using sse2 for ftoi"
+#if defined(__has_warning)
+#  if __has_warning("-Wunknown-pragmas")
+#    pragma clang diagnostic push
+#    pragma clang diagnostic ignored "-Wunknown-pragmas"
+#    pragma clang warning "using sse2 for ftoi"
+#    pragma clang diagnostic pop
+#  endif
+#endif
 #  define FPU_CONTROL
 
 typedef int16_t fpu_control;
