Index: sanlock-5.1.0/src/main.c
===================================================================
--- sanlock-5.1.0.orig/src/main.c
+++ sanlock-5.1.0/src/main.c
@@ -2455,8 +2455,8 @@ static int read_command_line(int argc, c
 	}
 
 	if (!strcmp(arg1, "--version") || !strcmp(arg1, "-V")) {
-		printf("%s %s (built %s %s)\n",
-		       argv[0], VERSION, __DATE__, __TIME__);
+		printf("%s %s\n",
+		       argv[0], VERSION);
 		exit(EXIT_SUCCESS);
 	}
 
