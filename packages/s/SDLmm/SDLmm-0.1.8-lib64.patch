--- configure.in
+++ configure.in
@@ -86,7 +86,7 @@
 
 case "$target" in
     *-*-linux*)
-      SDLMM_RLD_FLAGS="-Wl,-rpath,\${exec_prefix}/lib"
+      SDLMM_RLD_FLAGS="-Wl,-rpath,\${libdir}"
       ;;
     *-*-freebsd*)
       SDLMM_RLD_FLAGS="-Wl,-rpath,\${exec_prefix}/lib"
--- sdlmm-config.in
+++ sdlmm-config.in
@@ -1,6 +1,7 @@
 #!/bin/sh
 
 prefix=@prefix@
+libdir=@libdir@
 exec_prefix=@exec_prefix@
 exec_prefix_set=no
 
