Use the "checkin.sh" script to generate sesdev.spec and tarball from a git repo
and branch.  For example:

    $ bash checkin.sh --repo https://github.com/foo/sesdev --branch wip-foo

For more options, try "./checkin.sh --help"
