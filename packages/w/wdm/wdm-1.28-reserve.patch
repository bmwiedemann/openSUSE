--- src/wdm/dm.c
+++ src/wdm/dm.c
@@ -264,7 +264,8 @@
 	    len = strlen (lineBuf);
 	    if (lineBuf[len-1] == '\n')
 		lineBuf[len-1] = '\0';
-	    ParseDisplay (lineBuf, acceptableTypes, NumTypes);
+            if (!strstr(lineBuf,"reserve"))
+                ParseDisplay (lineBuf, acceptableTypes, NumTypes);
 	}
 	fclose (serversFile);
     }
