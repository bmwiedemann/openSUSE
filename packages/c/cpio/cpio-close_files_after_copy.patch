Index: b/src/copyin.c
===================================================================
--- a/src/copyin.c
+++ b/src/copyin.c
@@ -1595,6 +1595,19 @@ process_copy_in (void)
 
   cpio_file_stat_free (&file_hdr);
 
+  if (tty_in)
+  {
+    fclose(tty_in);
+  }
+  if (tty_out)
+  {
+    fclose(tty_out);
+  }
+  if (rename_in)
+  {
+    fclose(rename_in);
+  }
+
   if (append_flag)
     return;
 
