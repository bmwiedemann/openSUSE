Index: calibre-9.12.0/setup/build.py
===================================================================
--- calibre-9.12.0.orig/setup/build.py
+++ calibre-9.12.0/setup/build.py
@@ -470,6 +470,8 @@ class Build(Command):
                 continue
             if not is_ext_allowed(self.compiling_for, ext):
                 continue
+            if ext.name == 'piper':
+                continue
             if ext.error:
                 if ext.optional:
                     self.warn(ext.error)
Index: calibre-9.12.0/setup/commands.py
===================================================================
--- calibre-9.12.0.orig/setup/commands.py
+++ calibre-9.12.0/setup/commands.py
@@ -33,7 +33,6 @@ __all__ = [
     'manual',
     'mathjax',
     'osx',
-    'piper_voices',
     'pot',
     'publish',
     'publish_betas',
@@ -98,10 +97,6 @@ from setup.hyphenation import Hyphenatio
 
 hyphenation = Hyphenation()
 
-from setup.piper import PiperVoices
-
-piper_voices = PiperVoices()
-
 from setup.liberation import LiberationFonts
 
 liberation_fonts = LiberationFonts()
Index: calibre-9.12.0/setup/resources.py
===================================================================
--- calibre-9.12.0.orig/setup/resources.py
+++ calibre-9.12.0/setup/resources.py
@@ -116,7 +116,7 @@ class RapydScript(Command):  # {{{
 
 class Resources(Command):  # {{{
     description = 'Compile various needed calibre resources'
-    sub_commands = ['liberation_fonts', 'mathjax', 'rapydscript', 'hyphenation', 'piper_voices']
+    sub_commands = ['liberation_fonts', 'mathjax', 'rapydscript', 'hyphenation']
 
     def run(self, opts):
         from calibre.utils.serialize import msgpack_dumps
