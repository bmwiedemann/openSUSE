--- qore-sqlite3-module-1.0.1/config.guess.orig	2021-04-13 17:46:44.440924443 +0200
+++ qore-sqlite3-module-1.0.1/config.guess	2021-04-13 17:46:14.680675557 +0200
@@ -871,6 +871,9 @@ EOF
 	if test "$?" = 0 ; then LIBC="libc1" ; else LIBC="" ; fi
 	echo ${UNAME_MACHINE}-unknown-linux-gnu${LIBC}
 	exit ;;
+    aarch64:Linux:*:*)
+	echo aarch64-unknown-linux-gnu
+	exit ;;
     arm*:Linux:*:*)
 	eval $set_cc_for_build
 	if echo __ARM_EABI__ | $CC_FOR_BUILD -E - 2>/dev/null \
