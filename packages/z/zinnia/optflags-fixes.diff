---
 configure.in |    4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

--- a/configure.in
+++ b/configure.in
@@ -63,8 +63,8 @@
 dnl check gcc
 dnl
 if test -n "$GCC"; then
-   CFLAGS="-O3 -Wall"
-   CXXFLAGS="-O3 -Wall"
+   CFLAGS="$RPM_OPT_FLAGS -O3 -Wall"
+   CXXFLAGS="$RPM_OPT_FLAGS -O3 -Wall"
    AC_MSG_RESULT($ac_cv_x86_sse)
 fi
 
