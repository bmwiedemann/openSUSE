--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -17,4 +17,4 @@ CHECK_C_COMPILER_FLAG(-Oz COMPILER_SUPPORTS_OZ)
 if(COMPILER_SUPPORTS_OZ)
     add_definitions("-Oz")
 endif()
-set (CMAKE_SHARED_LINKER_FLAGS "-fuse-ld=lld -Wl,-z,max-page-size=16384,-z,common-page-size=16384,-z,relro,-z,now")
+set (CMAKE_SHARED_LINKER_FLAGS "-Wl,-z,max-page-size=16384,-z,common-page-size=16384,-z,relro,-z,now")
