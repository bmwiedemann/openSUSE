<services>
  <!-- only reenable if the addons get removed from the main blender tarball again -->
  <!--
  <service name="tar_scm" mode="manual">
    <param name="scm">git</param>
    <param name="url">https://projects.blender.org/blender/blender-addons.git</param>
    <param name="revision">v4.1.1</param>
    <param name="versionformat">@PARENT_TAG@</param>
    <param name="versionrewrite-pattern">v(.*)</param>
  </service>
  <service name="recompress" mode="manual">
    <param name="compression">xz</param>
    <param name="file">blender-addons*.tar</param>
  </service>
  -->

  <service name="tar_scm" mode="manual">
    <param name="scm">git</param>
    <param name="url">https://projects.blender.org/blender/blender-addons-contrib.git</param>
    <param name="revision">v4.1.1</param>
    <!--
    <param name="versionformat">@PARENT_TAG@</param>
    4.1.1 tag points to the same revision as 4.1.0 and so @PARENT_TAG@ returns 4.1.0
    -->
    <param name="versionformat">4.1.1</param>
    <param name="versionrewrite-pattern">v(.*)</param>
  </service>
  <service name="recompress" mode="manual">
    <param name="compression">xz</param>
    <param name="file">blender-addons-contrib*.tar</param>
  </service>
</services>
