apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: default
spec:
  containers:
  - name: busybox
    image: registry.opensuse.org/opensuse/busybox
    command:
      - pause
    imagePullPolicy: Always
  restartPolicy: Always
