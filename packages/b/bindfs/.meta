<package name="bindfs" project="openSUSE:Factory">
  <title>Mount Directories to other Locations and alter Permission Bits</title>
  <description>bindfs is a FUSE filesystem for mounting a directory to another location,
similarly to mount --bind. The permissions inside the mountpoint can be altered
using various rules.

</description>
  <devel project="filesystems" package="bindfs"/>
  <url>http://code.google.com/p/bindfs/</url>
</package>
