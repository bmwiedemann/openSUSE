<package name="kubectl-view-secret" project="openSUSE:Factory">
  <title>Kubernetes CLI plugin to decode Kubernetes secrets</title>
  <description>This plugin allows for easy secret decoding. Useful if you want to see what's inside of a secret without always go through the following:

* kubectl get secret &lt;secret&gt; -o yaml
* Copy base64 encoded secret
* echo &quot;b64string&quot; | base64 -d</description>
  <devel project="devel:kubic" package="kubectl-view-secret"/>
  <url>https://github.com/elsesiy/kubectl-view-secret</url>
</package>
