--- a/packages/script/src/index.ts
+++ b/packages/script/src/index.ts
@@ -14,7 +14,10 @@
 const expectedBunVersionRange = `^${expectedBunVersion}`
 
 if (!semver.satisfies(process.versions.bun, expectedBunVersionRange)) {
-  throw new Error(`This script requires bun@${expectedBunVersionRange}, but you are using bun@${process.versions.bun}`)
+  // A distribution ships one bun and cannot pin a second one just to build
+  // this. The check is upstream's convenience, not a compatibility barrier:
+  // the build is plain Bun.build() plus shell calls. Warn instead of failing.
+  console.warn(`warning: expected bun@${expectedBunVersionRange}, using bun@${process.versions.bun}`)
 }
 
 const env = {
