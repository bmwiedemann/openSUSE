Index: bin/hollywood
===================================================================
--- bin/hollywood.orig
+++ bin/hollywood
@@ -43,7 +43,7 @@ spin_up() {
 	done
 }
 
-WIDGET_DIR="$(dirname $0)/../lib/$PKG"
+WIDGET_DIR="$(dirname $0)/../libexec/$PKG"
 SPLITS=$(ls "$WIDGET_DIR" | wc -l)
 DELAY=10
 while [ ! -z "$1" ]; do
