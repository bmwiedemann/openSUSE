References: gh#HaxeFoundation/hashlink#582
Index: hashlink-1.13/libs/sdl/sdl.c
===================================================================
--- hashlink-1.13.orig/libs/sdl/sdl.c
+++ hashlink-1.13/libs/sdl/sdl.c
@@ -428,7 +428,7 @@ HL_PRIM void HL_NAME(set_window_grab)(SD
 }
 
 HL_PRIM bool HL_NAME(get_window_grab)(SDL_Window* window) {
-	SDL_GetWindowGrab(window);
+	return SDL_GetWindowGrab(window);
 }
 
 HL_PRIM const char *HL_NAME(detect_keyboard_layout)() {
